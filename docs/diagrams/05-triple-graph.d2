# Triple Graph Example

alice: Alice {
  shape: circle
}

fastapi: FastAPI {
  shape: circle
}

postgresql: PostgreSQL {
  shape: circle
}

jwt: JWT {
  shape: circle
}

api: API Service {
  shape: circle
}

alice -> fastapi: uses
fastapi -> postgresql: stores data in
fastapi -> jwt: implements auth with
api -> fastapi: built with
api -> postgresql: depends on
